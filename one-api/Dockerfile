# FROM calciumion/neko-api:main
FROM justsong/one-api
EXPOSE 3000
ENV PORT 3000
ENV BATCH_UPDATE_ENABLED=true
ENV BATCH_UPDATE_INTERVAL=120
ENV GLOBAL_WEB_RATE_LIMIT=600
ENV GLOBAL_API_RATE_LIMIT=1800

# ENV CHANNEL_UPDATE_FREQUENCY=60
# ENV CHANNEL_TEST_FREQUENCY=70
# ENV POLLING_INTERVAL=5

WORKDIR /data
RUN mkdir /data/logs && chmod 777 /data/logs
ENTRYPOINT ["/one-api"]